let frutas = ["Maçã", "Banana", "Uva", "Pera"];

// lenght = conta quantos elementos existem na array.
// console.log(frutas.length);

for (let i = 0; i < frutas.length; i++) {
    console.log(`- ${frutas[i]}`); // Listando os elementos na array. (template string)
}
